# Agent notes

## Distribution

- **The app is distributed via TestFlight.** A `git push` to GitHub does NOT update the installed app — changes only appear after a new EAS build is submitted to TestFlight and the user installs the new build.
- When the user asks "will I see this?" or similar, remind them a new TestFlight build is required.
